---
phase: 02-realtime-gateway-dashboard-parity
plan: 06
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - .planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md
autonomous: false
requirements: [TEST-02]

must_haves:
  truths:
    - "The full backend and frontend test suites are green and both workspaces typecheck before any manual verification is attempted"
    - "A human confirms, in a real browser, that ticker, order book, balance and chart all render and update live from an end user's point of view"
    - "A human confirms the reconnection banner appears with the last known values still visible when the backend is restarted mid-session"
  artifacts:
    - path: ".planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md"
      provides: "signed-off validation contract with every row green and nyquist_compliant: true"
      contains: "nyquist_compliant"
  key_links:
    - from: "02-VALIDATION.md Per-Task Verification Map"
      to: "the six automated test files delivered by Plans 02-01 to 02-05"
      via: "each row's automated command re-run and marked green"
      pattern: "vitest run"
---

<objective>
Close the phase with the manual UAT that ROADMAP success criterion #4 and requirement TEST-02 demand: a human looking at a real browser, confirming the dashboard behaves like a trading dashboard.

Purpose: the automated suites prove the wiring; only a person watching the screen can confirm the live experience. This plan contains no implementation — it is the verification gate.
Output: a green full-suite run and a signed-off `02-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md
@.planning/phases/02-realtime-gateway-dashboard-parity/02-UI-SPEC.md
@.planning/phases/02-realtime-gateway-dashboard-parity/02-CONTEXT.md
@.planning/phases/02-realtime-gateway-dashboard-parity/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression run and validation contract sign-off</name>
  <files>.planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md (the Per-Task Verification Map rows to mark, and the Validation Sign-Off checklist)
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-01-SUMMARY.md through 02-05-SUMMARY.md (what actually shipped versus what each row claims)
  </read_first>
  <action>
Run the full suites in both workspaces (`cd backend && npx vitest run && npx tsc --noEmit`, then `cd frontend && npx vitest run && npx tsc -b --noEmit && npm run build`). Run `cd backend && npm audit --audit-level=high` and confirm zero high/critical advisories (the four known moderate dev-only drizzle-kit/esbuild advisories documented in `01-01-SUMMARY.md` remain accepted).

Update `02-VALIDATION.md`: set every row's `File Exists` column to ✅ and `Status` to ✅ green for the rows whose commands now pass, correct any command path that drifted during execution, and set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter. If any row is red, stop and report it instead of marking it green — a red row means the phase is not done, not that the row is wrong.
  </action>
  <verify>
    <automated>cd backend && npx vitest run && npx tsc --noEmit && cd ../frontend && npx vitest run && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both `npx vitest run` invocations exit 0 with no skipped realtime tests
    - Both typecheck commands exit 0 and `cd frontend && npm run build` exits 0
    - `cd backend && npm audit --audit-level=high` reports zero high/critical advisories
    - `grep -n "nyquist_compliant: true" .planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md` matches
    - `grep -c "⬜ pending" .planning/phases/02-realtime-gateway-dashboard-parity/02-VALIDATION.md` returns 0
  </acceptance_criteria>
  <done>Every automated row in the validation contract is green and recorded as such.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual dashboard UAT in a real browser (TEST-02)</name>
  <read_first>
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-UI-SPEC.md (the copy and visual contract the human checks against)
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-CONTEXT.md (D-01 to D-08 — the decisions this UAT confirms were honored)
  </read_first>
  <action>
Boot both workspaces, walk the verification script below (driving it with Claude Browser where possible), and PAUSE for the operator's verdict. Modify no source file in this task — if a defect is reported, record it verbatim in the summary with the step number it failed at and stop, so it can be planned as gap-closure work instead of patched inline.
  </action>
  <acceptance_criteria>
    - Each of the eight verification steps below is explicitly confirmed or explicitly reported as failed
    - Any reported defect is recorded verbatim in `02-06-SUMMARY.md` together with its failing step number
  </acceptance_criteria>
  <what-built>
Plans 02-01 to 02-05 delivered: an authenticated, topic-scoped WebSocket gateway at `GET /realtime`; live mini ticker, 10-level order book and full-asset balance panels; a searchable, persisted pair selector; and a TradingView chart that re-initializes on pair change. All of it is covered by automated tests — this step confirms the live experience a test cannot see.
  </what-built>
  <how-to-verify>
1. Start the backend: `cd backend && npx tsx src/server.ts` (confirm it logs a successful boot and does not crash if Binance is briefly unreachable).
2. Start the frontend: `cd frontend && npm run dev`, then open the printed URL.
3. Log in with the seeded operator account.
4. On the dashboard, confirm within ~10 seconds and WITHOUT reloading the page:
   - the mini ticker price changes on its own, and its color shifts green/red as it moves;
   - the order book shows 10 bid rows (green) and 10 ask rows (red), updating continuously, with columns aligned;
   - the balance panel lists your account's assets — including zero-balance assets, which is intentional (D-03) — and scrolls;
   - the TradingView chart renders for the selected pair.
5. Open the pair dropdown, search for a different pair (e.g. type "ETH"), and select it. Confirm: the ticker and order book switch to the new pair, and the chart re-renders for the new pair.
6. Reload the page and confirm the pair you selected is still selected (D-08).
7. Open the browser devtools Network tab, filter to WS, and confirm the `/realtime` connection URL contains NO token query parameter and that the first frame sent is the auth message (SEC-06).
8. Confirm all on-screen copy is in Portuguese and matches 02-UI-SPEC.md.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all eight steps, including the devtools check that the `/realtime` URL carries no token parameter</human-check>
  </verify>
  <done>The live dashboard is confirmed by a human to render and update ticker, order book, balance and chart, with pair switching and persistence working.</done>
  <resume-signal>Type "approved" or describe exactly what did not render/update correctly</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual reconnection UAT — banner appears, data stays visible (D-04)</name>
  <read_first>
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-CONTEXT.md (D-04: banner is discreet and the last known values stay visible)
    - .planning/phases/02-realtime-gateway-dashboard-parity/02-UI-SPEC.md (the exact soft and hard-failure banner copy)
  </read_first>
  <action>
Drive the disconnect/reconnect script below against the running system and PAUSE for the operator's verdict. Modify no source file in this task — report any deviation verbatim rather than fixing it here.
  </action>
  <acceptance_criteria>
    - Each of the five steps below is explicitly confirmed or explicitly reported as failed
    - Any deviation from D-04 (blanked data, modal banner, required reload or re-login) is recorded verbatim in `02-06-SUMMARY.md`
  </acceptance_criteria>
  <what-built>
The browser WebSocket client re-authenticates and re-subscribes on every reconnect with exponential backoff, while the dashboard keeps rendering the last known ticker/book/balance values behind a discreet banner instead of blanking out.
  </what-built>
  <how-to-verify>
1. With the dashboard open and updating, stop the backend process (Ctrl+C).
2. Confirm within a few seconds: a discreet "Reconectando..." banner appears, the page does NOT blank out, and the last ticker price, order book rows and balance rows all remain on screen (frozen, not zeroed, not replaced by skeletons).
3. Leave it disconnected for ~30 seconds and confirm the app remains usable and eventually shows the hard-failure copy "Conexão perdida. Os dados exibidos podem estar desatualizados. Tentando novamente..." without becoming modal or blocking.
4. Restart the backend (`cd backend && npx tsx src/server.ts`).
5. Confirm the banner disappears on its own (no page reload, no re-login) and live updates resume for the same pair you had selected.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the banner appears, data stays frozen-but-visible, and recovery happens with no reload and no re-login</human-check>
  </verify>
  <done>D-04 reconnection behavior is confirmed by a human against a real backend restart.</done>
  <resume-signal>Type "approved" or describe what the UI did instead</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| running application → human observer | This plan verifies, in the live system, the boundaries the earlier plans implemented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01v | Information Disclosure | `/realtime` connection URL | mitigate | UAT step 7 has the human read the live WS URL in devtools and confirm no token query parameter is present — the runtime confirmation of SEC-06 that no unit test can provide |
| T-02-22 | Information Disclosure | balance panel on screen | accept | Real account balances are displayed to the authenticated operator on their own machine during UAT; this is the feature's purpose, and the operator should avoid screen-sharing the panel |
| T-02-SC | Tampering | dependencies at phase close | mitigate | `npm audit --audit-level=high` re-run in Task 1 confirms no high/critical advisory was introduced by this phase's installs |
</threat_model>

<verification>
- Both workspaces' full suites, typechecks and the frontend production build pass (Task 1)
- Both human checkpoints returned "approved"
- `02-VALIDATION.md` has no `⬜ pending` rows and `nyquist_compliant: true`
</verification>

<success_criteria>
- ROADMAP Phase 2 success criteria 1-4 are all observably true in a running system
- TEST-02 is satisfied by a recorded, approved manual browser UAT covering both the live dashboard and the reconnection behavior
</success_criteria>

<output>
Create `.planning/phases/02-realtime-gateway-dashboard-parity/02-06-SUMMARY.md` when done
</output>
